[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"

[project]
name = 'yamplan'
version = '0.0.2'
description = ''
readme = 'README.md'
requires-python = '>=3.8'
license = {file = 'LICENSE'}
authors = [
    {name = 'Kateřina Medvědová'},
]
dependencies = [
    "PyYAML"
]

[project.scripts]
yamplan = "yamplan.main:main"
